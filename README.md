You can compile the code using following bash commands:-

as -o server.o server.s (creates the object file from the source file)

ld -o server server.o (links the server.o to an executable named server)
